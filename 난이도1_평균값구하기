T = int(input())

for test_case in range(1, T + 1):
    t = test_case
    n = 0
    ave= 0
    a = map(int, input().split())

    b = list(a)

    for num in range(0,10):
        n += b[num]
        ave = round(n/10)

    print("#"+str(t),ave)
