T = int(input())

for test_case in range(1, T + 1):
    t = test_case

    a = map(int, input().split())

    b = list(a)

    if b[0] < b[1]:
        print("#" + str(t),"<")
    elif b[0] > b[1]:
        print("#" + str(t), ">")
    else:
        print("#" + str(t), "=")
